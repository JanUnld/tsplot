{% macro visibility(m) -%}
  {{- '+' if m.isPublic -}}
  {{- '#' if m.isProtected -}}
  {{- '-' if m.isPrivate -}}
{%- endmacro %}

{% macro edge(edge, connection = '--') -%}
  {{ edge.from.uniqueName }}{{ connection }}{{ edge.to.uniqueName }}
{%- endmacro %}
