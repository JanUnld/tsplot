{% set brackets = forceBrackets or member.fields.length or member.methods.length -%}

{{ kind or 'interface' }} {{ member.name }} {{ plantUml.stereotype(member.decorators) if member.decorators.length -}}
{{ '{' if brackets }}
{% block content %}
  {% block fields %}
    {% for f in member.fields %}
      {{- plantUml.field(f) | indent(4, true) }}
    {% endfor %}
  {% endblock %}
  {% block methods %}
    {% for m in member.methods %}
      {{- plantUml.signature(m) | indent(4, true) }}
    {% endfor %}
  {% endblock %}
{% endblock %}
{{ '}' if brackets }}
