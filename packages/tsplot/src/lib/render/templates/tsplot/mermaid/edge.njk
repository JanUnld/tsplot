{% extends 'tsplot/common/edge.njk' -%}

{% block association %}
  {{- common.edge(edge, '..') | indent(indentSize, true) }}
{% endblock %}
{% block composition %}
  {{- common.edge(edge, '--*') | indent(indentSize, true) }}
{% endblock %}
{% block aggregation %}
  {{- common.edge(edge, '--o') | indent(indentSize, true) }}
{% endblock %}
{% block classLike %}
  {{- common.edge(edge, '-->') | indent(indentSize, true) }}
{% endblock %}
{% block default %}
  {{- common.edge(edge, '..>') | indent(indentSize, true) }}
{% endblock %}
